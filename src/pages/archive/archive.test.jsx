import { describe, test, expect } from "vitest";
import Archive from "./archive";

describe("Archive", () => {
  test("component exists", () => {
    expect(Archive).toBeDefined();
    expect(typeof Archive).toBe("function");
  });

  test.todo("Add more tests for Archive");
});
